#include <random>
#include <iostream>
using namespace std;

char randDNA(int seed, string bases, int n)
{
	int n, i;
	Random_device rd;
	Mt19937 eng1 (rd()); // random seed
	Mt19937 eng2 (seed);
	
	letters [] = {'C','G','A','T'};
	
	for (i=0, i<8, i++ 
	
	
}
